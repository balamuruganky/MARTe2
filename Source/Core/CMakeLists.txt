ADD_SUBDIRS()

#
# Build MARTe2.so library
#
set (LIB_LIST ${PROJECT_NAME}${BAREMETAL_SUFFIX} 
              ${PROJECT_NAME}${FILESYSTEM_SUFFIX} 
              ${PROJECT_NAME}${SCHEDULER_SUFFIX}
              ${PLATFORM_LIBS})
COMBINE_SHARED_LIBS(${PROJECT_NAME} "${LIB_LIST}")