ADD_SUBDIRS()

#
# Build MARTe2Test.so library
#
set(TEST_EXEUTABLE_SUFFIX  Test)
set (LIB_LIST ${PROJECT_NAME}${TEST_BAREMETAL_SUFFIX} 
              ${PROJECT_NAME}${TEST_FILESYSTEM_SUFFIX}
              ${PROJECT_NAME}${TEST_SCHEDULER_SUFFIX})
COMBINE_SHARED_LIBS(${PROJECT_NAME}${TEST_EXEUTABLE_SUFFIX} "${LIB_LIST}")